"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state; // undefined (value is not yet assign)

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value its not zero (its type of object)
// undefined => 
// symbol => unique(used mostly in react give uniqunessto each)


// object
//typeof use to know type of data type

console.log(typeof undefined); // undefined
console.log(typeof null); // object